name: Авто-тест симулятора чачки
on:
  push:
    branches: ["main", "development", "feature/tests"]
  pull_request:
    branches: ["main", "development", "feature/tests"]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Чтение репозитория
        uses: actions/checkout@v7.0.1

      - name: Установка Python 3.14
        uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.14"

      - name: Установка всех зависимостей [1 / 7]
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Установка инструментов (Ruff, MyPy, Bandit, Pytest) [2 / 7]
        run: |
          python -m pip install mypy bandit ruff pytest

      - name: Проверка синтаксиса  (Ruff) [3 / 7]
        run: |
          ruff check . --select=E9,F401,F821,F841,E225

      - name: Проверка порядка импортов (Ruff) [4 / 7]
        run: |
          ruff check . --select=I

      - name: Проверка типов (MyPy) [5 / 7]
        run: |
          mypy . --ignore-missing-imports

      - name: Проверка уязвимостей (Bandit) [6 / 7]
        run: |
          bandit -r . -ll
      - name: Python-Тесты (pytest) [7 / 7]
        run: |
          pytest tests/
